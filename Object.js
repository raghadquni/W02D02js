const checkValues = (obj, value) => {
    const indexOfFirst = obj.indexOf(value);
    console.log(`The index of the first "${value}" from the beginning is ${indexOfFirst}`);

};
  

const totalCharacters = (arr) => {
  let ne =  arr.length;
  




 